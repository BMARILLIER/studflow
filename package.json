{
  "name": "studflow",
  "version": "1.0.0",
  "private": true,
  "description": "StudFlow — Ton coach pour reussir le bac",
  "scripts": {
    "dev": "vite",
    "build": "vite build && cp service-worker.js manifest.json dist/ && cp -r icons lib dist/",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "jsdom": "^25.0.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "@sentry/browser": "^10.46.0"
  }
}
